spring:
  cloud:
    config:
      name: config-client
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: SPRING-CLOUD-CONFIG-SERVER
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7000/eureka/